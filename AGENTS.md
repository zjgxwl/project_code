# Project Instructions

- Only modify files inside `project_code`.
- Do not modify `neu-master-thesis-template-main` or any LaTeX thesis files.
- This repository is for future implementations of thesis Chapter 3 TSPR, Chapter 4 TCSM, and Chapter 5 EGRO.
- Do not run full training jobs.
- Use real datasets with small subsets for tests and validation.
- Only download datasets when a command explicitly passes `--download`.
- After finishing, report the modified files and the commands that were run.
